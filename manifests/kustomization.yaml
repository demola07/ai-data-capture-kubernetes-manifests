apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml

# Optional: Add common labels
labels:
  - pairs:
      app: ai-data-capture-api
      managed-by: argocd
